#the file must be renamed to bootstrap.properties 
# the prefix of config files in the git repo
# should have the value of spring.application.name
#eg: limits-services-dev    

spring.application.name=limits-service
#server.port=
#limit-service.min =10
#limit-service.max=10000
spring.cloud.config=http://localhost:8888
spring.profiles.active=test